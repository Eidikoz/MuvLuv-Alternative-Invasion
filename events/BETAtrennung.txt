
add_namespace = BETAtrennung

country_event = {
	id = BETAtrennung.1
	title = "BETAtrennung.1.name"
	desc = "BETAtennung.1.desc"
	hidden =yes

	trigger = {
		tag =BET
		OR ={
			BET={ is_puppet = yes }
			has_government = fascism
			has_government = communism
			has_government = democratic
		}
		NOT = { 		
			has_game_rule = {
				rule = BET_spawn
				option = INFECTION
			}
		}
	}
	mean_time_to_happen = { days = 5 }

	option = {
		name = england
		random_neighbor_country = {
			annex_country = { target = BET transfer_troops = no }
		}
	}
}

country_event = {
	id = BETAtrennung.2
	title = "BETAtrennung.2.name"
	desc = "BETAtennung.2.desc"
	hidden =yes

	trigger = {
		tag =BET
		OR ={
			BET={ is_puppet = yes }
			has_government = fascism
			has_government = communism
			has_government = democratic
		}		
		has_game_rule = {
			rule = BET_spawn
			option = INFECTION
		}
	}
	mean_time_to_happen = { days = 5 }

	option = {
		name = england
		BRA = {
			transfer_state = 499
			transfer_state = 498
			transfer_state = 497
		}
		SPR = {
			transfer_state = 789
			transfer_state = 173
			transfer_state = 169
			transfer_state = 170
		}
		BET = {
		    release = YEM
			release = IRQ 	
			release = SAU 
			release = OMA 
			release = HON 
			release = NIC 
			release = COS 
			release = PAN 			
		}
		random_neighbor_country = {
			annex_country = { target = BET transfer_troops = no }
		}
	}
}
